# vSphere 인증 정보
# 이 파일을 /etc/basphere/vsphere.env로 복사하고 실제 값을 입력하세요.
# 주의: 이 파일은 root만 읽을 수 있도록 권한 설정 필요 (chmod 600)

# vCenter 인증 정보
VSPHERE_USER="administrator@vsphere.local"
VSPHERE_PASSWORD="your-password-here"

# TLS 설정 (자체 서명 인증서 사용 시)
VSPHERE_ALLOW_UNVERIFIED_SSL="true"
