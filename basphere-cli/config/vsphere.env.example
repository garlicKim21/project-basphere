# vSphere 인증 정보
# 이 파일을 /etc/basphere/vsphere.env로 복사하고 실제 값을 입력하세요.
# 주의: 이 파일은 root만 읽을 수 있도록 권한 설정 필요 (chmod 640)
#
# 비밀번호에 특수문자($, !, 등)가 있는 경우 작은따옴표 사용 권장
# 예: export VSPHERE_PASSWORD='Pa$$w0rd!'

# vCenter 인증 정보
export VSPHERE_USER='administrator@vsphere.local'
export VSPHERE_PASSWORD='your-password-here'

# TLS 설정 (자체 서명 인증서 사용 시)
export VSPHERE_ALLOW_UNVERIFIED_SSL='true'
